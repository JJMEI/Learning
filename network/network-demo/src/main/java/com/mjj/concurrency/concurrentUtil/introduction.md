```text


   并发工具类
   CountDownLatch
   
   Semaphore 信号量
        可以控制同时访问特定资源的线程数量，通过协调各个线程，以保证合理的使用资源
        1. 访问特定的资源前，必须使用acquire方法获得许可，如果许可数量为0，该线程
           一直阻塞，直到有可用许可
        2. 访问资源后，使用release释放许可
```