





        提交任务
           |
           |
           V
    <核心线程池是否已满> --<是>--> <队列是否已满> --<是>--> <线程池是否已满> --<是>--> 按照策略处理无法执行的任务【RejectedExecutionHandler】
           |                       |                      |
          <否>                    <否>                   <否>
           |                       |                      |
           V                       V                      V
     创建线程执行任务         将任务存储在队列中         创建线程执行任务






    1> 如果当前运行的线程少于corePoolSize，则创建新线程来执行任务（执行这一步需要全局锁）
    2> 如果运行的线程数大于或等于corePoolSize,则将任务加入到工作队列，如果工作队列满则check当前线程数是否达到最大数，没有则创建新的线程执行任务，有则执行拒绝策略